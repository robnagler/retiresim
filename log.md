# Log

## 2026-07-08

* 19:37:25 **Prompt**: "drop the cents from the output. has to be there to get consistent accounting"
* 19:37:25 **Update**: Changed `Bookkeeper.report()` to display whole dollars (`toFixed(0)` instead of `toFixed(2)`) -- display only, internal balances/arithmetic still carry full cent precision everywhere else. Updated the report test's regexes in [test/Bookkeeper.test.js](test/Bookkeeper.test.js). 42 tests passing.
* 19:32:54 **Prompt**: "bookkeper is constructed from config like everything else. it then reads accountClasses in its config which are just names." (sent mid-turn while fixing the log sort order)
* 19:32:54 **Update**: Folded the just-added static `Bookkeeper.fromConfig()` into the regular constructor -- `constructor({ config, classes })` now calls `super({ config })` (so `this.cfg = config.get('Bookkeeper')` via the same Base mechanism every other class uses) and builds `this.accounts` from `this.cfg.accountClasses` directly; the old `constructor({ accounts })` direct-injection path is gone. [src/main.js](src/main.js) updated to `new Bookkeeper({ config, classes })`. This forced every test file that built a Bookkeeper directly (Bookkeeper, Simulator, TraditionalIra, SecurityAccount, TaxCalculator, Cash) to switch from constructing accounts by hand to giving each a `Bookkeeper: { accountClasses: [...] }` config slice plus a `classes` registry, and reading the constructed instances back via `bookkeeper.accounts[i]` instead of holding a separately-built reference. Also had to add `class: 'Account'`/`class: 'TraditionalIra'`/`class: 'SecurityAccount'` to several config slices whose instance name (`Taxable`, `TradIra`, `Security`) didn't match the class being resolved. 42 tests passing; `main.js` output unchanged.
* 19:30:09 **Prompt**: "the log is not sorted properly"
* 19:30:09 **Feedback**: The 2026-07-08 section had drifted out of strict newest-first order (some Update entries were appended below older Prompt entries instead of inserted at the top). Rewrote the section in correct descending-timestamp order, with same-timestamp Update/Feedback/Creation entries kept above their companion Prompt entry.
* 19:29:33 **Update**: Moved the config-driven account-construction loop from [src/main.js](src/main.js) into a new static `Bookkeeper.fromConfig({ config, classes })` in [src/Bookkeeper.js](src/Bookkeeper.js) -- reads `config.get('Bookkeeper').accountClasses`, resolves each name's class the same way (`class:` field or fallback to the name), builds the accounts, and returns `new Bookkeeper({ accounts })`. Left the plain `constructor({ accounts })` untouched so every other test file (which hand-builds small account lists directly) needed no changes. main.js now just calls `Bookkeeper.fromConfig({ config, classes })`. Added a `fromConfig` test to [test/Bookkeeper.test.js](test/Bookkeeper.test.js) (42 tests total, all passing); `main.js` output unchanged.
* 19:29:33 **Prompt**: "bookeeper should do that not main"
* 19:28:05 **Update**: Simplified [src/main.js](src/main.js) -- no library code changed. Added a `classes` map (class name string -> imported class) and a `Bookkeeper: { accountClasses: [...] }` config slice listing the account instance names in build order. Replaced the four hardcoded `new TaxableAccount(...)`/`new TraditionalIra(...)`/etc. lines with a single loop over `config.get('Bookkeeper').accountClasses` that looks up each name's config slice, resolves the class via `c.class ?? name` (explicit `class:` field when the instance name differs from its class, e.g. `Taxable` -> `TaxableAccount`; falls back to the name itself when they match, e.g. `Mortgage`), and constructs it -- building the `accounts` array passed to `new Bookkeeper({ accounts })`. `Simulator` and `Bookkeeper` themselves stay as explicit literal constructions, not part of the loop. 41 tests still passing; `main.js` output unchanged.
* 19:28:05 **Prompt**: "let's get simpler. The config can have a class: TaxableAccount. if it doesn't have a class, then the class is the name (Simulator). then you can create all the objects in a loop with simulator and bokkeper being explicit in main.js. indeed the bookkeeper's config could be the order of the classes accountClasses: [\"TaxableAccount\", \"TradiitionaIra\", etc. ]. it then just creaets all the accounts from the config and cretes the accounts arg"
* 19:22:40 **Update**: Moved `rate`/`priority`/`basis`/`monthlyPayment` out of constructor arguments and into config, keyed by instance name (falling back to class name when no name is given) so two same-class objects (e.g. two Mortgages) can carry different config. [src/Base.js](src/Base.js) now sets `this.name` (if given) before the `cfg` lookup, and looks up `config.get(this.name ?? this.constructor.name)`. [src/Account.js](src/Account.js), [src/Mortgage.js](src/Mortgage.js), [src/TaxableAccount.js](src/TaxableAccount.js), [src/Cash.js](src/Cash.js), [src/Simulator.js](src/Simulator.js) all simplified to just `super({ name, config })` (or `super({ config })` for Simulator) and pull their own fields from `this.cfg`. Updated every test file and [src/main.js](src/main.js): config slices are now keyed by each instance's chosen name (`Taxable`, `TradIra`, `Tax`, `Security`) rather than always the class name, and `rate`/`priority`/`basis`/`monthlyPayment`/`withdraw` moved from constructor call sites into their config slices. 41 tests still passing; `main.js` output unchanged.
* 19:21:17 **Update**: Added a `Total` row (sum of every account's balance) to `Bookkeeper.report()`. Updated [test/Bookkeeper.test.js](test/Bookkeeper.test.js)'s report test. 41 tests still passing; `main.js` now prints a Total line.
* 19:21:17 **Prompt**: "add a total to the report"
* 19:18:05 **Prompt**: "seems like all those arguments to the objects should be config for those objects. we'll always name the objects so we can talk about two different mortgages, for example. so if the object as a name (this.name) use it else use constructor name to get the config."
* 19:13:45 **Prompt**: "run main and show output"
* 19:13:30 **Feedback**: Simplified the error-message regexes in [test/TraditionalIra.test.js](test/TraditionalIra.test.js) and [test/Account.test.js](test/Account.test.js) from asserting the full `class=X cfg=... name=Y balance=Z` field dump down to just `/amount=1001.*balance=1000/` -- the observed values, not the full context, matching the error-message-style rule. 41 tests still passing.
* 19:13:08 **Prompt**: "so for the trad ira you don't really need all the context in there just amount=1001.*balance=1000. That's sufficient."
* 19:11:20 **Update**: Moved the `config.get(this.constructor.name)` -> `this.cfg` lookup out of [src/Account.js](src/Account.js) and into [src/Base.js](src/Base.js)'s constructor (accepts an optional `config` param; skips setting `cfg` if omitted, so `JournalEntry`/`Posting`/`Bookkeeper`/`Config` -- which call `super()` with no args -- are unaffected). `Account` and `Simulator` now just `super(config)` and read `this.cfg`. This shifts `cfg` to the first field in `toString()`'s field dump (since Base's constructor runs before the subclass's own field assignments) -- updated the two regex tests in [test/Account.test.js](test/Account.test.js) and [test/TraditionalIra.test.js](test/TraditionalIra.test.js) that asserted the old `class=X name=Y balance=Z` adjacency. 41 tests still passing; `main.js` still runs end to end.
* 19:10:08 **Prompt**: "base should accept config"
* 19:09:40 **Update**: Changed [src/Simulator.js](src/Simulator.js) to `extends Base` for consistent `toString()`-based error messages, matching every other class. 41 tests still passing.
* 19:09:26 **Prompt**: "simulator should use base"
* 19:09:15 **Update**: Changed [src/Simulator.js](src/Simulator.js)'s constructor from `{ bookkeeper, startYear, endYear }` to `{ bookkeeper, config }` -- reads `config.get('Simulator')` for `startYear`/`endYear`, matching the same `config.get(this.constructor.name)` pattern every Account subclass already uses. Updated [test/Simulator.test.js](test/Simulator.test.js) accordingly. Added [src/main.js](src/main.js) -- builds one hardcoded Config (Simulator + TaxableAccount + TraditionalIra + Mortgage + LivingExpense slices), constructs those accounts, hands the same config to both Bookkeeper's accounts and Simulator, runs a 5-year simulation, and prints `bookkeeper.report()`. Added `"start": "node src/main.js"` to package.json. Did not wire Cash/TaxCalculator into main.js -- Simulator/Bookkeeper still don't drive those explicitly, that's a separate open gap. 41 tests still passing; `npm start` runs end to end.
* 19:08:03 **Prompt**: "we need a main program. might as well be simulator. it has to supply a config which can be hardcoded for now. it should run through some set of years that are configured so simulator should get config in the construtor not start/end year. the main should create the bookkeeper and hand it to the simulator. all with the same config"
* 19:04:20 **Creation**: Added `Bookkeeper.report()` -- dumps each account's name and current balance as a padded text table (`Account`/`Balance` header, one row per account in `this.accounts` order). Added a test in [test/Bookkeeper.test.js](test/Bookkeeper.test.js) (41 tests total, all passing).
* 19:03:41 **Prompt**: "let's create bookkeeper output for test, just another function that dumps accounts, balances, etc. in a table"
* 19:02:10 **Prompt**: "where are we"

## 2026-07-07

* 20:22:45 **Feedback**: "gckp" -- ran tests, committed as `fdee82f cash spend/produce, living expense, tax as account`, pushed to origin/main. Session ending here.
* 20:22:24 **Prompt**: "gkcp and stopping"
* 20:21:28 **Prompt**: "perfect"
* 20:20:54 **Prompt**: "it's not in the log. when i give a prompt, store it literally with the current time stamp then do the prompt"
* 20:20:54 **Feedback**: Corrected the logging rule -- entries must contain the verbatim prompt text (not a paraphrase), logged before the work starts. Updated the project memory (`feedback_log_every_prompt.md`) accordingly.
* 20:20:54 **Prompt**: "are you recording my primpts?"
* 20:19:34 **Update**: Resolved the spend/produce ordering question -- balances are allowed to go negative (like a mortgage's debt), so [src/Cash.js](src/Cash.js) now `spend()`s first (carrying Cash negative by the total owed) then `produce({ amount: -this.balance })` to bring it back to zero, matching "spend creates the amount for produce" literally. Overrode `Cash.withdraw()` to skip the insufficient-funds guard (only Cash; other account types still enforce it). Replaced the now-obsolete "spend throws when cash cannot cover" test in [test/Cash.test.js](test/Cash.test.js) with one confirming Cash can go negative and still reconciles.
* 20:15:39 **Creation**: Added [src/LivingExpense.js](src/LivingExpense.js) (`extends Account`, `due()` returns `{ account: this.name, amount: this.balance }`) -- living expenses are inflation-adjusted annually per CLAUDE.md, so this reuses `Account.grow()`/`rate` directly instead of a bare number. Simplified [src/Cash.js](src/Cash.js)'s `runYear()` to just sum every spender's `due()` (dropped the special-cased `this.cfg.livingExpense`) -- `LivingExpense` is now just another `spenders` entry alongside `Mortgage` and `TaxCalculator`. Updated [test/Cash.test.js](test/Cash.test.js), added [test/LivingExpense.test.js](test/LivingExpense.test.js) (40 tests total, all passing).
* 20:14:26 **Update**: Changed [src/TaxCalculator.js](src/TaxCalculator.js) to `extends Account` instead of `Base` -- it holds a balance with a configured initial value just like any account, so it fits Account's own constructor pattern with no custom code needed. Config field `priorYear` -> `balance`; instance field `this.priorYear` -> `this.balance`; `due()` now returns `{ account: this.name, amount: this.balance }` (was a hardcoded `'Tax'` string). Still called explicitly, not registered in `Bookkeeper.accounts`, so no automatic reconciliation applies to it yet. Updated [test/TaxCalculator.test.js](test/TaxCalculator.test.js) and [test/Cash.test.js](test/Cash.test.js).
* 20:10:15 **Feedback**: Renamed `Cash.spend()`'s `category` param to `account` -- it was actually flowing into `Posting.account`, not `JournalEntry.category`, so the name was misleading; `spend()` now posts a fixed `JournalEntry.category: 'spend'` and takes `account` for the actual nominal destination. Updated `TaxCalculator.due()`/`Mortgage.due()` to return `{ account, amount }` instead of `{ category, amount }` to match. Updated [test/Cash.test.js](test/Cash.test.js), [test/Mortgage.test.js](test/Mortgage.test.js), [test/TaxCalculator.test.js](test/TaxCalculator.test.js).
* 20:03:44 **Update**: Wired the "first pass of the year" together. [src/TaxCalculator.js](src/TaxCalculator.js): renamed `settle` to `runYear({ year, bookkeeper })` -- reads this year's posted `'OrdinaryIncome'`, sets `this.priorYear` for next year, returns nothing (called explicitly at end-of-year, not from Bookkeeper's per-account loop, since it isn't itself a balance-holding account). Added `due()` returning `{ category: 'Tax', amount: this.priorYear }`. [src/Mortgage.js](src/Mortgage.js): `makePayment()` now stores `this.interest`; added `due()` returning `{ category: 'MortgageInterest', amount: this.interest }` -- finally gives mortgage interest a funding path (previously computed and discarded). [src/Cash.js](src/Cash.js): added a `spenders` constructor param and `runYear({ year, bookkeeper })` that calls `due()` on every spender plus `this.cfg.livingExpense`, sums to a total, `produce()`s it from the withdrawal order, then `spend()`s it out per category. The `due()` interface (uniform `{category, amount}` shape) was my own inference to make "call all spending objects" concrete -- flagged for the user to confirm/correct. Updated [test/TaxCalculator.test.js](test/TaxCalculator.test.js), [test/Mortgage.test.js](test/Mortgage.test.js), [test/Cash.test.js](test/Cash.test.js) (37 tests total, all passing).
* 19:56:55 **Feedback**: Renamed `settle()`'s local `d` to `rv` in [src/TaxCalculator.js](src/TaxCalculator.js) -- `rv` is reserved for the return-value variable.
* 19:54:50 **Update**: Renamed [src/TaxCalculator.js](src/TaxCalculator.js)'s config field `startingTax` and instance field `this.owed` to `priorYear` -- the value seeded at construction and carried forward by `settle()`. Updated [test/TaxCalculator.test.js](test/TaxCalculator.test.js).
* 19:52:35 **Rename**: Renamed `CashGenerator` to `Cash` (`git mv src/CashGenerator.js src/Cash.js`, `git mv test/CashGenerator.test.js test/Cash.test.js`); config key auto-follows via `this.constructor.name` (now `'Cash'` instead of `'CashGenerator'`). Added `livingExpense` to Cash's own config slice.
* 19:50:08 **Update**: Renamed [src/CashGenerator.js](src/CashGenerator.js)'s `raise()` to `produce()` and added the parallel `spend({ amount, category, year, bookkeeper })` -- withdraws from Cash's own balance and posts `[Cash, -amount]/[category, +amount]`. This sets up the planned "first pass of the year": sum mortgage interest + living expenses (config still TBD) + prior year's tax into a spending total, `produce()` it into Cash from the withdrawal order, then `spend()` it out to each expense category -- not yet wired together. Updated [test/CashGenerator.test.js](test/CashGenerator.test.js) accordingly.
* 19:44:00 **Update**: Reworked [src/CashGenerator.js](src/CashGenerator.js) into a real `Account` (a zero-rate clearing/cash account with `name`/`balance` from config) so `Bookkeeper` can cycle and reconcile it like any other account. `raise({ amount, year, bookkeeper })` now posts a proper two-posting `JournalEntry` per source withdrawal (`[source, -w]`/`[Cash, +w]`, category `${sourceClassName}Withdrawal`) instead of silently mutating balances -- confirmed with the user that the source account's withdrawal is bound to its own posting/entry, carrying that source's tax character. Updated [test/CashGenerator.test.js](test/CashGenerator.test.js) to verify both balances and journal postings via a real `Bookkeeper`.
* 19:39:57 **Feedback**: "gckp" -- ran tests, committed as `2ce83ef tax calculator and cash generator`, pushed to origin/main.
* 19:39:19 **Creation**: Added [src/CashGenerator.js](src/CashGenerator.js) -- a standalone class that raises a target cash amount by withdrawing from accounts in a configured `withdrawalOrder` list (e.g. `CashGenerator: { withdrawalOrder: ['Taxable', 'TradIra'] }`), stopping once covered, throwing with the shortfall if the listed accounts can't cover it. This is deliberately just the account-selection mechanism -- full income-based optimization (RMDs, netting out already-received income like SS) is future work; `raise(amount)` currently expects the caller to have already netted out any known income. Not yet wired into Bookkeeper/Simulator/TaxCalculator. Added [test/CashGenerator.test.js](test/CashGenerator.test.js).
* 19:34:34 **Discussion/Update**: Dropped the same-year tax gross-up/delta idea (too complicated) in favor of paying this year's tax from the prior year's computed liability, seeded by a configured `startingTax` for year one -- avoids the circular estimate-then-true-up problem entirely by accepting a one-year lag. Added [src/TaxCalculator.js](src/TaxCalculator.js)'s `settle(income)`: returns `this.owed` (amount due this year, carried from last call or `startingTax`), then updates `this.owed` to `calculate(income).total` for next year. Updated [test/TaxCalculator.test.js](test/TaxCalculator.test.js). Still not wired into Bookkeeper/Simulator -- which account funds the actual payment remains open, same as before.
* 19:22:00 **Feedback**: Changed `federal()`'s bracket loop in [src/TaxCalculator.js](src/TaxCalculator.js) to `break` once `u <= p` (income fully absorbed by prior brackets) instead of skipping the no-op with an `if (u > p)` wrapper -- stops iterating remaining brackets entirely rather than checking each one.
* 19:17:41 **Feedback**: Renamed `federal()`'s accumulator local `t` to `rv` in [src/TaxCalculator.js](src/TaxCalculator.js), consistent with `rv` being reserved for the return-value variable.
* 19:15:33 **Feedback**: Renamed `this.config` (the per-class slice) to `this.cfg` in [src/Account.js](src/Account.js), [src/TaxCalculator.js](src/TaxCalculator.js), and [src/TraditionalIra.js](src/TraditionalIra.js), to visually distinguish "my own narrow config slice" from the `config` constructor parameter (the full `Config` instance).
* 19:11:08 **Feedback**: Removed the `federal`/`state` named temporaries in [src/TaxCalculator.js](src/TaxCalculator.js)'s `calculate()`, using the reserved `rv` return-value object instead (`rv.total = rv.federal + rv.state`).
* 19:09:51 **Update**: Renamed [src/TaxCalculator.js](src/TaxCalculator.js)'s `colorado()`/`coloradoRate` to `state()`/`stateRate` -- flat-rate state tax generalized beyond Colorado specifically (works for any simple flat-rate state, not progressive-bracket states). Updated [test/TaxCalculator.test.js](test/TaxCalculator.test.js) to match.
* 19:07:00 **Creation**: Added [src/TaxCalculator.js](src/TaxCalculator.js) (not yet committed) -- first cut of build-order step 6, federal (progressive brackets from config) + Colorado (flat rate from config) income tax, `calculate(income)` returns `{ federal, colorado, total }`; a pure calculator, not yet wired into the yearly Simulator/Bookkeeper loop since taxes still need a funding account once withdrawals-to-cover-expenses (step 7) exists. Added [test/TaxCalculator.test.js](test/TaxCalculator.test.js).
* 19:04:59 **Feedback**: Promoted "gckp" from a project-only memory to a global one (`~/.claude/memory/tools/git.md`), scoped to simple personal projects with no PR workflow (like rnfinance) as opposed to RadiaSoft/pykern projects, which use branch-per-issue + PRs instead.
* 19:03:52 **Feedback**: Defined "gckp" as shorthand for "commit and push all pending files with a short message"; ran tests, committed the opening-balances-from-config change plus log.md as `53dc9e3 opening balances from config; log.md`, and pushed to origin/main. Saved the shorthand as a project reference memory.
* 19:01:03 **Feedback**: Told to log every prompt going forward, not just backfill history -- every user turn in this project now gets a `log.md` entry with a real timestamp (via the `date` shell command) and the actions taken, including pure discussion turns with no file changes; saved as a project memory (`feedback_log_every_prompt.md`).
* 19:01:03 **Discussion**: Confirmed `log.md` was only a one-time backfill so far, not yet kept current turn-by-turn (in answer to "are you logging everything?").
* 19:01:03 **Discussion**: Confirmed `Account` already looks up its config by the instantiating subclass's own name, not literally "Account" -- `this.constructor` resolves polymorphically at runtime, so `this.constructor.name` is `'Mortgage'`, `'TaxableAccount'`, etc. depending on what was actually constructed.
* 19:01:03 **Update**: Generalized opening balances into Config at the Account base level (not yet committed) -- [src/Account.js](src/Account.js)'s constructor now takes `config` instead of `balance` directly and reads `this.balance` from `config.get(this.constructor.name).balance`; [src/TraditionalIra.js](src/TraditionalIra.js) no longer needs its own constructor; [src/Mortgage.js](src/Mortgage.js) and [src/TaxableAccount.js](src/TaxableAccount.js) updated to pass `config` through (basis stays a direct param for now); all 8 test files updated to build a `Config` with each account's opening balance.
* 18:24:34 **Feedback**: Iterated JournalEntry.js's error messages down to a terse "prose + x=y for observed values only, never expected values, no repeated words, no restated location" style (final: `not two postings=${postings}` and `non-zero sum=${t} postings=${postings}`); saved as a project memory and, at request, also as a global memory under `~/.claude/memory/feedback_error_messages.md` so it applies to any project.
* 18:24:34 **Feedback**: Inlined [src/JournalEntry.js](src/JournalEntry.js)'s two nested check functions (checkCount/checkBalance) directly into the constructor as guard clauses -- "too complicated with functions... reverse tests so early exit."
* 18:24:34 **Update**: Updated [src/TraditionalIra.js](src/TraditionalIra.js) and [test/TraditionalIra.test.js](test/TraditionalIra.test.js) for the new Config shape.
* 18:24:34 **Feedback**: Reworked Config into pure name -> data storage with no method names tied to any module's concepts (`get(name)`, no validation) so each module decides its own config shape and looks itself up by class name (`config.get(this.constructor.name)`).
* 18:24:34 **Creation**: Added [src/Config.js](src/Config.js) and [test/Config.test.js](test/Config.test.js) as a first cut, a `withdrawal(accountName, year)` lookup, then wired it into TraditionalIra's constructor and runYear().
* 18:24:34 **Discussion**: Worked through what "TraditionalIra withdraws something in a year" requires -- a nominal income counter-account to post against (like UnrealizedGrowth/MortgagePrincipalPaid), and a way to specify how much to withdraw in a given year, since withdrawals aren't autonomous like growth or a fixed mortgage payment.
* 17:54:44 **Creation**: Added [src/SecurityAccount.js](src/SecurityAccount.js) (extends TaxableAccount; runYear() is a no-op so it never grows or posts) and [test/SecurityAccount.test.js](test/SecurityAccount.test.js).
* 17:54:44 **Discussion**: Designed SecurityAccount as a fixed-cost-basis holding (basis set once at construction, assumed not to grow, everything treated as LTCG) to track stock sales without full basis-growth bookkeeping.
* 17:54:44 **Creation**: Added [src/TraditionalIra.js](src/TraditionalIra.js) (extends Account, no basis; withdraw() returns `{ balance, income: amount }`) and [test/TraditionalIra.test.js](test/TraditionalIra.test.js).
* 17:54:44 **Discussion**: Deferred tagging withdrawals with a tax "type" (that belongs on JournalEntry.category, posted by whoever calls withdraw(), not on withdraw() itself) in favor of building TraditionalIra first, since its withdrawals are simpler -- the whole amount is ordinary income, no basis tracking.
* 17:47:30 **Creation**: Added [src/TaxableAccount.js](src/TaxableAccount.js) (extends Account; basis tracked via deposit()/withdraw() overrides that keep basis proportional to balance; `_checkBasis()` guards the basis-never-exceeds-balance invariant) and [test/TaxableAccount.test.js](test/TaxableAccount.test.js).
* 17:47:30 **Discussion**: Reviewed the remaining build order and chose to start step 1's remaining Account subclasses with TaxableAccount.
* 17:42:39 **Creation**: Added [test/Simulator.test.js](test/Simulator.test.js) -- a multi-year run over an Account + Mortgage verifying balances and reconciling every year, plus a test that a broken runYear override makes Simulator.run() throw immediately with the failing year. Closed build-order step 4 (end-to-end pure-growth reconciliation).

## 2026-07-04

* 16:00:19 **Feedback**: Renamed Bookkeeper._reconcile's nested parameter from `a` to `account` (parameters get informative names) while keeping the outer loop local `a` (locals stay single-letter); made the same fix in Simulator.run()'s loop.
* 16:00:19 **Update**: Rewrote [test/Bookkeeper.test.js](test/Bookkeeper.test.js) to exercise real Account/Mortgage instances through `bookkeeper.runYear()` instead of fakes.
* 16:00:19 **Update**: Added `runYear()` to [src/Account.js](src/Account.js) (grows by rate, posts a 'growth' JournalEntry) and [src/Mortgage.js](src/Mortgage.js) (pays down principal, posts a 'mortgagePrincipal' JournalEntry).
* 16:00:19 **Update**: Reworked [src/Bookkeeper.js](src/Bookkeeper.js) to own accounts (sorted by priority) and drive `runYear(year)` itself (snapshot -> run each account -> reconcile).
* 16:00:19 **Creation**: Added [src/Simulator.js](src/Simulator.js) (`{ bookkeeper, startYear, endYear }`, `run()` loops `bookkeeper.runYear(year)`).
* 16:00:19 **Discussion**: Redesigned ownership so each Account implements its own runYear() and reports to whichever Bookkeeper it's given; Bookkeeper owns the account list/ordering/reconciliation; Simulator is reduced to a thin multi-year loop.
* 16:00:19 **Feedback**: Fixed a third hand-edit bug in Mortgage.js (`rv.interest:` instead of `rv.interest =`, a syntax error).
* 15:41:09 **Feedback**: Adopted the convention that local variables (including loop/nested-closure locals) are single letters while parameters get descriptive names, and that `rv` is reserved for the return-value variable; reindented to 4 spaces, no tabs.
* 15:41:09 **Feedback**: Fixed two bugs introduced while hand-editing Mortgage.js (missing `const r`, and referencing an undefined `p` instead of the newly-computed `principal`), reproducing each with a failing test before fixing per the bug-fix-needs-reproduction rule.
* 15:41:09 **Creation**: Added [test/Account.test.js](test/Account.test.js) and [test/Mortgage.test.js](test/Mortgage.test.js).
* 15:41:09 **Creation**: Added [src/Mortgage.js](src/Mortgage.js) (extends Account; caches growthFactor/yearlyPayment; makePayment() splits principal/interest).
* 15:41:09 **Update**: Reworked [src/Account.js](src/Account.js), [src/Bookkeeper.js](src/Bookkeeper.js), [src/JournalEntry.js](src/JournalEntry.js), [src/Posting.js](src/Posting.js) to extend Base and use full-context error messages.
* 15:41:09 **Creation**: Added [src/Base.js](src/Base.js) (generic `toString()` dumping all fields) to shorten error messages.
* 15:41:09 **Discussion**: Decided Mortgage should extend Account, using a closed-form fixed-rate amortization formula (monthly compounding, fixed yearly payment) rather than a naive annual approximation or a 12-iteration loop.
* 14:59:51 **Creation**: Added [src/Account.js](src/Account.js) (balance, grow, deposit, withdraw) -- first cut, before Mortgage or runYear existed.
* 14:56:54 **Creation**: Added [test/Bookkeeper.test.js](test/Bookkeeper.test.js) as a bootstrap test, later rewritten to use real accounts.
* 14:56:54 **Creation**: Added [src/Bookkeeper.js](src/Bookkeeper.js) (journal array, `post()`, `balanceChange()`).
* 14:56:54 **Creation**: Added [src/JournalEntry.js](src/JournalEntry.js) (exactly two Postings, validated to sum to zero at construction).
* 14:56:54 **Creation**: Added [src/Posting.js](src/Posting.js) (one side of an entry: account + signed amount).
* 14:56:54 **Discussion**: Agreed on double-entry bookkeeping (two Postings per JournalEntry, must sum to zero) for auditability, and on "class per file until unwieldy."
* 14:56:54 **Update**: Added a "Modularization" section (class list and 8-step build order) to [CLAUDE.md](CLAUDE.md).
* 14:56:54 **Creation**: Added package.json, index.html, .gitignore, and config/.gitignore scaffolding a plain Node project.
* 14:56:54 **Reset**: Wiped and reinitialized the local git repo (`git init --initial-branch=main`) ahead of manually force-pushing a cleared history to GitHub.
* 14:56:54 **Discussion**: Clarified plain-JS/Node conventions (node --test, ES modules, no bundler, no npm deps) after pivoting away from an earlier Python/pykern approach.
* 14:08:08 **Creation**: Added [README.md](README.md) as a placeholder before scaffolding the project.
